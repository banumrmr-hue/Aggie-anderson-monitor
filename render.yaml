services:
  - type: worker
    name: Aggieanderson-monitor
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_discord.py
    envVars:
      - key: DISCORD_BOT_TOKEN
        sync: false
      - key: BOT_NAME
        value: Aggie anderson monitor
